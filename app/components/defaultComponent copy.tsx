import React from 'react';

import {
  View,
  Text,
} from 'react-native';

import domisStyle from '../domisStyles';

type LoadIdProps = {
  
};

const DefaultComponent: React.FC<LoadIdProps> = ({}) => {
    return (
        <View>

        </View>
    )
}

export default DefaultComponent